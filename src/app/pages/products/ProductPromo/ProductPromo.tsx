import { Box } from '@mui/material';
import { FC } from 'react';

const ProductPromo: FC = () => {
  return <Box sx={{ position: 'absolute', left: 0, top: 16, bgcolor: '#F9A52B', padding: '4px 16px', color: '#fff' }}>Promo</Box>;
};

export default ProductPromo;
